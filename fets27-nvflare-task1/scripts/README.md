# Scripts

The main user-facing entrypoints live in:

- `setup.sh`
- `setup.ps1`
- `python -m fets27_challenge.cli ...`

This folder is reserved for organizer-side helper scripts if the repo grows beyond the initial simulator-first release.

