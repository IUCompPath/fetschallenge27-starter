Baseline checkpoints are generated here by:

```bash
python -m fets27_challenge.cli prepare-assets --data-root ./data/toy
```

The runtime can still start from the locked model architecture if the checkpoint files are not present, but organizers should generate and freeze these files for official runs.

